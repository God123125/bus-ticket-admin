export enum LocalStorageEnum {
  Token = '_t',
  Role = '_r',
  token_expires_at = '_tea',
  username = '_u',
  language = '_l',
  profile = '_pro',
  company_name = '_cn',
  company_image = '_ci',
  company_color = '_cc',
}
